docker run  --rm -it --privileged -v $(pwd):/opt  busybox sh -c 'mkdir /opt/media/ && mount -t vfat -o loop /opt/build/boot.img /opt/media/ && cp /opt/build/*.bin /opt/media/ && sync && umount /opt/media/ && rm -rf /opt/media/'
